# Installscript for mac and archlinux systems
I've created a installscript for installing fresh systems easier if I need to reinstall my computer.

Just run the command under this text. It will automatically detect if it's a MacOS or Arch system.

`sh install.sh`